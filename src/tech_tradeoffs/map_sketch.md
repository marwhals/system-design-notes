# Stuff map WIP

---

Stuff for building software systems.

---

```mermaid
graph LR
    subgraph Java
        Spring/Webflux
        Quarkus
    end

    subgraph Scala
        ZIO
        Cats/TypeLevel
        Akka_http
        Play
    end

    subgraph Python
        ML_Stuff
        MatplotLib
    end

    subgraph Message_Brokers
        Kafka
        Aeron
        end

    subgraph Databases
        PostgreSQL
        MongoDB
        Cassandra
        Redis
    end

    subgraph Frontend_Frameworks
        React
    end

    subgraph Data
        D3
        Spark
    end

    subgraph Stuff
        Linux
        Docker
        Kubernetes
        Jenkins
        Maven/Gradle/SBT
        Terraform/IAC
    end
```